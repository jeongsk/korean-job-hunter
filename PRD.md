# Job Hunter — Product Requirements Document

> Claude Code 플러그인 기반 AI 구직 도우미 · 서브에이전트 + 슬래시 명령어 아키텍처

---

## 1. 개요

### 1.1 프로젝트 배경

구직 활동은 반복적이고 시간 소모적이다. 채용공고를 여러 사이트에서 직접 탐색하고, 이력서와 얼마나 맞는지 수동으로 판단하며, 통근 거리나 재택 여부까지 일일이 확인해야 한다. **Job Hunter**는 이 과정을 AI 에이전트로 자동화하는 Claude Code 플러그인이다.

### 1.2 핵심 가치

- **시간 절약**: 공고 수집과 1차 필터링을 자동화
- **정확한 매칭**: 이력서 기반으로 기술 적합도를 정량적으로 분석
- **생활 조건 반영**: 통근 거리, 재택 근무 여부를 매칭 점수에 반영
- **Claude Code 생태계 통합**: 별도 런타임 없이 Claude Code 안에서 구직 워크플로우 실행
- **간편한 설치**: `claude plugin install` 한 줄로 누구나 설치 가능

### 1.3 아키텍처 전환 배경

기존 독립 실행형 CLI 도구(TypeScript + Bun + Commander.js) 설계에서 **Claude Code 플러그인**으로 전면 재설계한다. Claude Code 서브에이전트와 슬래시 명령어를 활용함으로써 별도 AI SDK 의존성 없이 Claude Code 자체의 오케스트레이션 기능을 그대로 사용할 수 있다.

---

## 2. 목표 및 성공 지표

| 목표 | 지표 | 측정 방법 |
|------|------|---------|
| 공고 수집 자동화 | 잡코리아 · 원티드 2개 소스 수집 성공률 ≥ 90% | 수집 시도 건수 대비 title/company/url 3개 필드 모두 추출 성공 비율 |
| 매칭 정확도 | 사용자 평가 기준 Top-5 추천 정확도 ≥ 70% | 사용자가 "지원예정"으로 표시한 공고가 추천 Top-5 내 포함 비율 |
| 슬래시 명령어 응답성 | 매칭 분석 1건 ≤ 30초 | `/job-match --job-id` 실행 시작 ~ 리포트 출력까지 wall clock 시간 |
| 이력서 파싱 | YAML/Markdown + PDF 두 형식 모두 지원 | 각 형식 10개 샘플 기준 필수 필드(이름, 스킬, 경력) 추출 성공 비율 |
| 위치 필터링 | 집 주소 기준 통근 시간 계산 오차 ≤ 15분 | 카카오맵 실제 경로 시간 대비 계산값 차이 (10개 주소 샘플) |
| 플러그인 설치 | 설치 후 `/job-search` 첫 실행까지 5분 이내 | README 가이드 기준 설치 소요 시간 |

---

## 3. 사용자 페르소나

**Primary: Claude Code 사용 개발자 구직자 (이주영, 3년차 백엔드)**
- Claude Code를 일상적으로 사용하며, 구직 활동도 같은 환경에서 처리하고 싶음
- 여러 구직 사이트를 탭 전환하며 수동으로 공고를 확인하는 것이 비효율적
- 이력서를 Markdown/YAML로 관리하거나 PDF 보유
- 서울 마포구 거주 — 출퇴근 1시간 이내, 또는 재택/하이브리드 선호

**Secondary: 취업 준비생**
- 공고가 많아 어디서부터 봐야 할지 막막함
- 자신의 스펙과 맞는 공고를 추려내고 싶음
- Claude Code 플러그인 생태계를 탐색 중인 사용자

---

## 4. 기능 요구사항

### 4.1 채용공고 수집 (`/job-search`)

**소스**:
- LinkedIn Jobs (⚠️ 공개 공고만 수집; 로그인 기반 스크래핑은 ToS 위반 위험 — Section 12 참고)
- 잡코리아 (jobkorea.co.kr)
- 원티드 (wanted.co.kr)

**동작 방식**:
- **Playwright CLI** (`playwright` 명령어, Bash 직접 실행)를 통한 웹 스크래핑 (scraper-agent)
- 키워드, 지역, 경력 조건으로 필터링
- **재택 근무 여부** 자동 추출 (전면재택 / 하이브리드 / 오피스)
- **사무실 주소** 자동 추출 → 통근 시간 계산
- 수집된 공고를 로컬 SQLite DB에 저장 (중복 제거)

**출력 예시**:
```
[12] 원티드 · 카카오 — 백엔드 엔지니어 (5년↑)   매칭: 87%  재택: 하이브리드  통근: 35분
[13] LinkedIn · 토스 — Node.js 개발자             매칭: 82%  재택: 전면재택    통근: -
[14] 잡코리아 · 네이버 — Java 서버 개발자          매칭: 71%  재택: 오피스      통근: 48분
```

### 4.2 이력서 관리 (`/job-resume`)

**형식 지원**:
- **YAML 마스터 이력서** (`data/resume/master.yaml`): 버전 관리 가능, 필드 구조화
- **PDF 업로드** (`data/resume/uploads/`): resume-agent가 텍스트 추출 후 YAML로 변환

**YAML 이력서 스키마 (요약)**:
```yaml
profile:
  name: 홍길동
  email: hong@example.com
  home_address: "서울시 마포구"    # 통근 거리 계산 기준
  commute_preferences:
    max_minutes: 60               # 최대 허용 통근 시간 (분)
    preferred_work_type:          # 근무 형태 우선순위
      - remote                   # 전면재택
      - hybrid                   # 하이브리드
      - onsite                   # 오피스

skills:
  languages: [TypeScript, Python, Java]
  frameworks: [NestJS, FastAPI, Spring]
  tools: [Docker, Kubernetes, PostgreSQL]

experience:
  - company: 스타트업A
    role: 백엔드 엔지니어
    period: 2022-01 ~ 현재
    highlights:
      - "MSA 전환으로 응답시간 40% 단축"

education:
  - school: 한국대학교
    major: 컴퓨터공학
    degree: 학사
    graduated: 2022-02
```

### 4.3 매칭 분석 (`/job-match`)

**동작**:
1. 이력서(YAML)와 채용공고 텍스트를 matcher-agent에 전달
2. 스킬 매칭, 경력 요건, 우대사항을 항목별로 분석
3. **통근 거리 점수**: 사무실 주소와 집 주소 기반 통근 시간 계산
4. **근무 형태 점수**: 재택/하이브리드/오피스와 이력서 선호도 비교
5. 종합 점수(0–100, 기술 70% + 위치 30%)와 분석 리포트 출력

**리포트 예시**:
```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
토스 · Node.js 개발자    종합: 85 / 100
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
[기술 매칭 — 60/70]  (스킬 86/100 기준, 가중치 적용)
✅ 핵심 스킬        TypeScript, Node.js, PostgreSQL
✅ 경력 요건        3년 이상 (현재 3년차)
⚠️  우대사항 부분    Kubernetes 경험 없음
❌ 미충족           MSA 설계 경험 언급 없음

[위치 · 근무 형태 — 25/30]
🏠 근무 형태        전면재택 ✅ (선호: 재택 우선)
📍 사무실 위치      서울 강남구 (통근 불필요 — 재택)
⏱️  예상 통근 시간   해당없음

[제안] 이력서에 분산 시스템 경험을 구체적으로 기술하세요.
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```

**점수 가중치**:
| 항목 | 가중치 | 설명 |
|------|--------|------|
| 기술 스킬 매칭 | 40% | 필수/우대 기술 일치도 |
| 경력 요건 | 20% | 요구 경력 충족 여부 |
| 우대사항 | 10% | 우대 조건 부합도 |
| 근무 형태 | 15% | 재택/하이브리드/오피스 일치 |
| 통근 거리 | 15% | 최대 허용 통근 시간 대비 실제 통근 시간 |

### 4.4 지원 현황 추적 (`/job-track`)

- 공고별 지원 상태 관리: `관심 / 지원예정 / 지원완료 / 서류탈락 / 면접 / 합격 / 불합격`
- 상태 변경 기록 및 메모 추가
- tracker-agent가 SQLite DB를 통해 CRUD 처리

---

## 5. 플러그인 설치

### 5.1 설치 방법

> **주의**: `claude plugin install` 명령어는 Claude Code 플러그인 마켓플레이스 공식 출시 이후 사용 가능. 현재는 저장소를 직접 클론 후 `.claude/` 경로를 프로젝트에 포함하는 방식으로 대체 가능 (Open Question #7 참고).

```bash
# GitHub 저장소에서 직접 설치 (마켓플레이스 출시 이후)
claude plugin install owner/job-hunter

# 마켓플레이스에서 설치 (등록 후)
claude plugin install @marketplace/job-hunter
```

### 5.2 설치 후 확인

```bash
# 슬래시 명령어 사용 시작
/job-resume add ./my-resume.pdf
/job-search --keyword "백엔드" --location "서울"
```

### 5.3 MCP 서버 설정 (선택)

SQLite 데이터베이스 접근을 위해 MCP 서버를 사용하는 경우:

```bash
cd mcp-server && npm install && npm run build
```

Claude Code `settings.json`에 MCP 서버 등록:

```json
{
  "mcpServers": {
    "job-hunter-db": {
      "command": "node",
      "args": ["./mcp-server/dist/server.js"],
      "cwd": "/path/to/job-hunter"
    }
  }
}
```

---

## 6. 기술 아키텍처

### 6.1 기술 스택 전환

| 레이어 | 기존 (CLI) | 신규 (Plugin) |
|--------|-----------|-------------|
| 인터페이스 | Commander.js CLI | Claude Code 슬래시 명령어 |
| AI 오케스트레이션 | Anthropic SDK 직접 호출 | Claude Code 서브에이전트 |
| 웹 스크래핑 | Playwright (독립 라이브러리) | Playwright CLI (Bash 직접 실행) |
| 데이터베이스 | better-sqlite3 | MCP 서버 또는 Bash 직접 접근 |
| 배포/설치 | npm/bun install | claude plugin install |
| 위치 서비스 | 없음 | 카카오맵 API |

### 6.2 에이전트 아키텍처

```
/job-search (슬래시 명령어)
      │
      ▼
Claude Code (내장 오케스트레이션)
      │
      ├── scraper-agent    ──→  Playwright CLI (Bash) → 공고 수집·구조화
      │                         재택 여부 + 사무실 주소 추출
      ├── resume-agent     ──→  파일 읽기 + PDF 파싱 → 이력서 프로필 생성
      ├── matcher-agent    ──→  이력서 × 공고 → 기술 점수 + 위치 점수
      └── tracker-agent    ──→  SQLite → 지원 현황 CRUD
```

**에이전트별 사양**:

| 에이전트 | 모델 | 사용 도구 | 주요 책임 |
|----------|------|-----------|---------|
| scraper-agent | claude-sonnet-4-6 | Read, Write, Bash, Glob, Grep | 공고 수집, 재택 여부, 주소 추출 |
| resume-agent | claude-sonnet-4-6 | Read, Write, Bash | PDF/YAML 파싱, 이력서 저장 |
| matcher-agent | claude-sonnet-4-6 | Read, Bash | 기술+위치 매칭 점수 산출 |
| tracker-agent | claude-haiku-4-5 | Read, Write, Bash | 지원 현황 CRUD |

### 6.3 데이터 모델 (SQLite)

```sql
-- 채용공고
CREATE TABLE jobs (
  id            TEXT PRIMARY KEY,
  source        TEXT,         -- 'linkedin' | 'jobkorea' | 'wanted'
  title         TEXT,
  company       TEXT,
  url           TEXT UNIQUE,
  content       TEXT,         -- 원문 JD
  location      TEXT,         -- 도시/지역
  office_address TEXT,        -- 사무실 상세 주소 (위치 계산용)
  work_type     TEXT,         -- 'remote' | 'hybrid' | 'onsite'
  commute_min   INTEGER,      -- 집에서 사무실까지 예상 통근 시간 (분)
  created_at    TEXT,
  fetched_at    TEXT
);

-- 매칭 결과
CREATE TABLE matches (
  id            TEXT PRIMARY KEY,
  job_id        TEXT REFERENCES jobs(id),
  resume_hash   TEXT,
  score         INTEGER,      -- 0–100 (기술 70% + 위치 30%)
  skill_score   INTEGER,      -- 기술 매칭 세부 점수
  location_score INTEGER,     -- 위치/근무형태 세부 점수
  report        TEXT,         -- JSON
  created_at    TEXT
);

-- 지원 현황
CREATE TABLE applications (
  id            TEXT PRIMARY KEY,
  job_id        TEXT REFERENCES jobs(id),
  status        TEXT,         -- 'interested' | 'applied' | 'interview' | ...
  memo          TEXT,
  updated_at    TEXT
);
```

---

## 7. 플러그인 구조

### 7.1 디렉토리 레이아웃

```
job-hunter/
├── .claude-plugin/
│   ├── plugin.json              # 플러그인 매니페스트
│   └── marketplace.json         # 마켓플레이스 등록 정보
├── .claude/
│   ├── agents/                  # 서브에이전트 정의
│   │   ├── scraper-agent.md     # 공고 수집 에이전트
│   │   ├── resume-agent.md      # 이력서 파싱 에이전트
│   │   ├── matcher-agent.md     # 매칭 분석 에이전트
│   │   └── tracker-agent.md    # 지원 현황 추적 에이전트
│   ├── commands/                # 슬래시 명령어
│   │   ├── job-search.md        # /job-search
│   │   ├── job-match.md         # /job-match
│   │   ├── job-track.md         # /job-track
│   │   └── job-resume.md        # /job-resume
│   └── skills/                  # 스킬 정의
│       ├── job-scraping/
│       │   └── SKILL.md
│       ├── job-matching/
│       │   └── SKILL.md
│       └── job-tracking/
│           └── SKILL.md
├── data/
│   ├── jobs.db                  # SQLite 데이터베이스
│   └── resume/
│       ├── master.yaml          # 마스터 이력서 (집 주소, 통근 선호 포함)
│       └── uploads/             # PDF 업로드 경로
├── mcp-server/                  # 선택적 MCP 서버
│   ├── server.ts
│   └── package.json
├── README.md
├── LICENSE
└── PRD.md
```

### 7.2 플러그인 매니페스트

**`.claude-plugin/plugin.json`**:
```json
{
  "name": "job-hunter",
  "description": "AI-powered job hunting assistant for Claude Code — search jobs, match resumes, track applications",
  "version": "1.0.0",
  "author": {
    "name": "jeongseok"
  },
  "skills": "./.claude/skills/"
}
```

**`.claude-plugin/marketplace.json`**:
```json
{
  "$schema": "https://anthropic.com/claude-code/marketplace.schema.json",
  "name": "job-hunter",
  "plugins": [{
    "name": "job-hunter",
    "description": "AI job hunting plugin: search, match resumes, track commute & remote work",
    "version": "1.0.0",
    "category": "productivity",
    "tags": ["job-search", "resume", "career", "remote-work", "commute"]
  }]
}
```

---

## 8. 슬래시 명령어 설계

### 8.1 명령어 목록

```
# 이력서 관리
/job-resume add ./my-resume.pdf
/job-resume add ./master.yaml
/job-resume show
/job-resume set-home "서울시 마포구 합정동"  # 집 주소 등록 (통근 계산 기준)

# 채용공고 검색
/job-search --keyword "백엔드" --location "서울" --sources wanted,linkedin
/job-search --keyword "Node.js" --min-match 70
/job-search --keyword "백엔드" --remote hybrid,remote    # 재택 근무 필터
/job-search --keyword "백엔드" --max-commute 60          # 통근 60분 이내 필터

# 매칭 분석
/job-match --top 10                  # 저장된 공고 중 상위 10개 매칭
/job-match --job-id abc123           # 특정 공고 상세 분석
/job-match --top 10 --remote-only    # 재택/하이브리드 공고만 매칭

# 지원 현황 관리
/job-track list
/job-track set abc123 --status applied --memo "1차 서류 제출"
/job-track set abc123 --status interview
```

### 8.2 명령어 파일 구조 (`.claude/commands/*.md`)

각 명령어는 YAML 프론트매터와 에이전트 위임 지침으로 구성:

```markdown
---
description: "Search job postings with keyword, location, remote work, and commute filters"
argument-hint: "--keyword <keyword> [--location <city>] [--remote remote|hybrid|onsite] [--max-commute <minutes>] [--sources wanted,linkedin,jobkorea]"
---

Use the scraper-agent sub-agent to search job postings.
Arguments: $ARGUMENTS
```

---

## 9. 서브에이전트 정의

각 에이전트는 `.claude/agents/` 에 마크다운 파일로 정의된다.

### 9.1 에이전트 파일 형식

```markdown
---
name: scraper-agent
description: "Collects job postings from LinkedIn, Jobkorea, Wanted. Extracts office address, work type (remote/hybrid/onsite), and estimates commute time from home address."
tools: Read, Write, Bash, Glob, Grep
model: sonnet
---

# Scraper Agent

You are a job posting collection specialist...
```

### 9.2 에이전트별 도구 매핑

| 에이전트 | 허용 도구 | 외부 의존성 |
|----------|---------|-----------|
| scraper-agent | Read, Write, Bash, Glob, Grep | playwright CLI (`npm i -g playwright`) |
| resume-agent | Read, Write, Bash | 없음 |
| matcher-agent | Read, Bash | 카카오맵 API (통근 계산) |
| tracker-agent | Read, Write, Bash | 없음 |

---

## 10. 스킬 정의

각 스킬은 `.claude/skills/{name}/SKILL.md` 형식으로 정의된다.

### 10.1 job-scraping

웹 스크래핑 워크플로우 스킬:
- Playwright CLI (`playwright` Bash 실행 패턴 및 인수 설계)
- rate limiting 준수 규칙
- 재택 근무 여부 텍스트 추출 패턴 (전면재택 / 하이브리드 / 오피스)
- 사무실 주소 추출 및 정규화 규칙

### 10.2 job-matching

이력서 매칭 분석 스킬:
- 기술 스킬 비교 알고리즘
- 경력 요건 평가 기준
- 통근 시간 점수 산출 공식
- 근무 형태 우선순위 매핑
- 종합 점수 가중치 계산 (기술 70% + 위치 30%)

### 10.3 job-tracking

지원 현황 관리 스킬:
- SQLite CRUD 패턴
- 상태 전이 규칙
- 메모 포맷 규칙

---

## 11. MCP 서버 (선택)

SQLite 데이터베이스 접근을 위한 선택적 MCP 서버.

**기술 스택**: TypeScript + `@modelcontextprotocol/sdk`

**제공 도구**:
- `db_save_job(job)` — 채용공고 저장
- `db_search_jobs(filters)` — 공고 검색 (재택 여부, 통근 시간 필터 포함)
- `db_save_match(match)` — 매칭 결과 저장
- `db_get_applications()` — 지원 현황 조회
- `db_update_application(id, status, memo)` — 지원 상태 업데이트

**MCP 서버 없는 경우 폴백**:
```bash
# Bash 도구로 SQLite 직접 접근
sqlite3 data/jobs.db "SELECT * FROM jobs WHERE work_type = 'remote'"
```

---

## 12. 비기능 요구사항

- **로컬 우선**: 모든 데이터는 로컬에 저장, 외부 서버 불필요
- **모델 비용 최적화**: 경량 작업(상태 업데이트)은 Haiku 모델, 분석 작업은 Sonnet 모델
- **스크래핑 안전성**: rate limiting 준수, 적절한 User-Agent 설정
- **API 키 불필요**: Claude Code 자체 인증 사용 (별도 ANTHROPIC_API_KEY 불필요)
- **위치 정보 보호**: 집 주소는 로컬 YAML에만 저장. 통근 시간 계산 시 카카오맵 API에 주소를 임시 전송하나 응답 수신 즉시 삭제 (원문 주소는 외부 서버에 저장되지 않음; 계산된 통근 시간(분)만 DB에 캐싱)
- **LinkedIn 스크래핑 제약**: 로그인 없이 접근 가능한 공개 공고만 수집. LinkedIn ToS 위반 위험으로 인해 로그인 기반 스크래핑은 기본 미지원; 수집 범위는 공개 검색 결과로 제한

---

## 13. 오류 처리 및 복원력

### 13.1 스크래핑 오류

| 오류 상황 | 처리 방식 |
|---------|---------|
| 페이지 로드 타임아웃 | 최대 3회 재시도 후 공고 스킵, `data/errors/scrape-errors.json` 에 기록 |
| 사이트 구조 변경 감지 | 경고 출력 후 수동 검토 요청 |
| 중복 공고 | URL UNIQUE 제약으로 자동 무시 (`INSERT OR IGNORE`) |
| 필수 필드 누락 (title/company/url) | 해당 공고 스킵, 오류 로그 기록 |
| 부분 데이터 수집 | 선택 필드 NULL 허용 후 저장 |

### 13.2 통근 계산 오류

| 오류 상황 | 처리 방식 |
|---------|---------|
| 카카오맵 API 레이트 리밋 | Exponential backoff 후 재시도 |
| 주소 파싱 실패 | `commute_min = NULL` 저장, 매칭 시 위치 점수 0점 처리 |
| API 키 미설정 | 통근 계산 전체 스킵, 근무 형태 점수(15점)만 반영 |

### 13.3 DB 오류

| 오류 상황 | 처리 방식 |
|---------|---------|
| SQLite 파일 잠금 | 단일 프로세스 순차 쓰기 (동시 쓰기 없음) |
| DB 파일 손상 | `data/jobs.db.bak` 백업 안내 메시지 출력 |
| 디스크 용량 부족 | 경고 출력 + 90일 이상 공고 자동 아카이브 제안 |

### 13.4 PDF 파싱 오류

| 오류 상황 | 처리 방식 |
|---------|---------|
| 스캔 이미지 PDF | OCR 불가 안내, 텍스트 복사본 업로드 요청 |
| 암호화된 PDF | 암호 해제 후 재업로드 안내 |
| 파싱 신뢰도 낮음 (필드 50% 미만 추출) | 사용자에게 YAML 직접 입력 권유 |

---

## 14. 구현 단계 (Milestones)

### M1 — 플러그인 기반 구축
- [ ] `.claude-plugin/plugin.json`, `marketplace.json` 작성
- [ ] 슬래시 명령어 4개 기본 파일 생성 (`.claude/commands/`)
- [ ] `data/` 디렉토리 및 SQLite 스키마 초기화
- [ ] YAML 이력서 스키마 정의 (집 주소, 통근 선호 필드 포함)

### M2 — 에이전트 및 스킬 정의
- [ ] 서브에이전트 4개 마크다운 파일 작성 (`.claude/agents/`)
- [ ] 스킬 3개 SKILL.md 파일 작성 (`.claude/skills/`)
- [ ] 이력서 파싱 로직 (resume-agent, `/job-resume add`)

### M3 — 스크래핑 통합
- [ ] scraper-agent 구현 (Playwright CLI 기반 웹 스크래핑)
- [ ] 원티드 / 잡코리아 스크래퍼 (재택 여부 + 주소 추출)
- [ ] LinkedIn 공개 공고 스크래퍼 (비로그인, 검색 결과 페이지만)
- [ ] 카카오맵 API 통근 시간 계산 로직 (사무실 주소 → 예상 통근 시간)
- [ ] 중복 제거 및 DB 저장 (오류 처리 포함 — Section 13 기준)

### M4 — 매칭/추적 통합 및 배포
- [ ] matcher-agent 구현 (기술 + 위치 점수 산출)
- [ ] tracker-agent 구현 (`/job-track` 명령어)
- [ ] README.md 작성 (설치 가이드, 사용법)
- [ ] GitHub 저장소 공개 및 마켓플레이스 등록 (Open Question #7 해결 후)

### M5 — MCP 서버 (선택)
- [ ] SQLite MCP 서버 구현 (`mcp-server/`)
- [ ] DB 도구 7개 (`db_save_job`, `db_search_jobs` 등)
- [ ] `.mcp.json` 설정 파일 작성

---

## 15. 미결 사항 (Open Questions)

### 해결된 사항

| # | 결정 내용 | 결과 |
|---|---------|------|
| 1 | ~~카카오맵 API vs Google Maps MCP~~ | **✅ 카카오맵 API** — 한국 주소 정확도 우수, 무료 티어로 충분 |
| 2 | ~~`claude-in-chrome` vs `mcp__playwright`~~ | **✅ Playwright CLI** — Bash 직접 실행, MCP 의존성 제거 |

### 미결 사항

| # | 질문 | 영향 |
|---|------|------|
| 3 | MCP 서버 없이 Bash + `sqlite3` CLI로 SQLite 직접 접근이 충분한가? | M5 |
| 4 | LinkedIn 공개 공고 스크래핑 범위 — 비로그인으로 수집 가능한 공고 수 및 필드 확인 | M3 |
| 5 | 이력서 여러 버전 관리 지원 여부 | M1 |
| 6 | 잡코리아 스크래핑 차단 대응 방안 (User-Agent 로테이션, 요청 간격 조정) | M3 |
| 7 | 마켓플레이스 등록 절차 및 `claude plugin install` 명령어 실제 지원 여부 확인 | M4 |
| 8 | 매칭 점수 가중치 (기술 70% / 위치 30%) 사용자 커스터마이징 지원 여부 | M2 |

---

*최초 작성: 2026-02-27*
*v2.0 고도화: 2026-02-27 — CLI → Claude Code Plugin 전환, 통근 거리 및 재택 근무 기능 추가*
*v2.1 개선: 2026-02-27 — Playwright CLI 전환, LinkedIn ToS 제약 명시, 개인정보 정책 수정, 점수 표기 일관성, 에이전트 도구 통일, Milestone 재정렬(M3↔M4), 오류 처리 섹션(13) 신설, Open Questions Q1/Q2 해결*
